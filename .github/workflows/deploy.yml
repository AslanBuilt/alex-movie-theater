name: Deploy to parityrfp.com

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  ftp-deploy:
    name: FTP Deploy to /cs/alex-movie-theater/
    runs-on: ubuntu-latest
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Lint PHP (syntax gate — aborts deploy before any upload)
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq php-cli
          php -v
          echo "Linting PHP files in public/ and scripts/ ..."
          find ./public ./scripts -name '*.php' -print0 | xargs -0 -n1 -P4 php -l
          echo "All PHP files passed php -l"

      - name: Generate database.php from secrets
        id: gen_db
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASS: ${{ secrets.DB_PASSWORD }}
        run: |
          python3 - << 'PYEOF'
          import os, sys

          def esc(v):
              return v.replace('\\', '\\\\').replace("'", "\\'")

          host = esc(os.environ.get('DB_HOST', ''))
          db   = esc(os.environ.get('DB_NAME', ''))
          user = esc(os.environ.get('DB_USER', ''))
          pwd  = esc(os.environ.get('DB_PASS', ''))

          if not host:
              sys.stdout.write("WARNING: DB secrets not configured — skipping database.php generation.\n")
              sys.stdout.write("Server will keep its existing database.php unchanged.\n")
              sys.stdout.write("Fix: set DB_HOST, DB_NAME, DB_USER, DB_PASSWORD in repo secrets.\n")
              with open('/tmp/.skip_db_upload', 'w') as f:
                  f.write('1')
          else:
              php = (
                  "<?php\n"
                  "declare(strict_types=1);\n"
                  "return [\n"
                  "    'host'     => '" + host + "',\n"
                  "    'database' => '" + db   + "',\n"
                  "    'username' => '" + user + "',\n"
                  "    'password' => '" + pwd  + "',\n"
                  "    'charset'  => 'utf8mb4',\n"
                  "];\n"
              )
              with open('./public/config/database.php', 'w') as f:
                  f.write(php)
              sys.stdout.write("Generated public/config/database.php from secrets\n")
          PYEOF

      - name: Generate stripe.php from secrets
        id: gen_stripe
        env:
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
          STRIPE_PUBLISHABLE_KEY: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}
          STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
          STRIPE_MODE: ${{ secrets.STRIPE_MODE }}
        run: |
          python3 - << 'PYEOF'
          import os, sys

          def esc(v):
              return v.replace('\\', '\\\\').replace("'", "\\'")

          sk   = esc(os.environ.get('STRIPE_SECRET_KEY', ''))
          pk   = esc(os.environ.get('STRIPE_PUBLISHABLE_KEY', ''))
          wh   = esc(os.environ.get('STRIPE_WEBHOOK_SECRET', ''))
          mode = esc(os.environ.get('STRIPE_MODE', '') or 'test')

          if not sk:
              sys.stdout.write("WARNING: STRIPE_SECRET_KEY not set — skipping stripe.php generation.\n")
              sys.stdout.write("Server keeps its existing stripe.php (Stripe checkout will error until set).\n")
              sys.stdout.write("Fix: set STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET in repo secrets.\n")
              with open('/tmp/.skip_stripe_upload', 'w') as f:
                  f.write('1')
          else:
              php = (
                  "<?php\n"
                  "declare(strict_types=1);\n"
                  "return [\n"
                  "    'secret_key'      => '" + sk + "',\n"
                  "    'publishable_key' => '" + pk + "',\n"
                  "    'webhook_secret'  => '" + wh + "',\n"
                  "    'currency'        => 'usd',\n"
                  "    'mode'            => '" + mode + "',\n"
                  "];\n"
              )
              with open('./public/config/stripe.php', 'w') as f:
                  f.write(php)
              sys.stdout.write("Generated public/config/stripe.php from secrets (mode=" + mode + ")\n")
          PYEOF

      - name: Generate sendgrid.php from secrets
        id: gen_sendgrid
        env:
          SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_FROM_NAME: ${{ secrets.MAIL_FROM_NAME }}
          MAIL_REPLY_TO: ${{ secrets.MAIL_REPLY_TO }}
        run: |
          python3 - << 'PYEOF'
          import os, sys

          def esc(v):
              return v.replace('\\', '\\\\').replace("'", "\\'")

          key = esc(os.environ.get('SENDGRID_API_KEY', ''))
          frm = esc(os.environ.get('MAIL_FROM', ''))
          nm  = esc(os.environ.get('MAIL_FROM_NAME', '') or 'The Alex')
          rep = esc(os.environ.get('MAIL_REPLY_TO', '') or os.environ.get('MAIL_FROM', ''))

          if not key or not frm:
              sys.stdout.write("WARNING: SENDGRID_API_KEY / MAIL_FROM not set — skipping sendgrid.php.\n")
              sys.stdout.write("Order-confirmation emails will safely no-op until these secrets are set.\n")
              with open('/tmp/.skip_sendgrid_upload', 'w') as f:
                  f.write('1')
          else:
              php = (
                  "<?php\n"
                  "declare(strict_types=1);\n"
                  "return [\n"
                  "    'api_key'   => '" + key + "',\n"
                  "    'from'      => '" + frm + "',\n"
                  "    'from_name' => '" + nm  + "',\n"
                  "    'reply_to'  => '" + rep + "',\n"
                  "];\n"
              )
              with open('./public/config/sendgrid.php', 'w') as f:
                  f.write(php)
              sys.stdout.write("Generated public/config/sendgrid.php from secrets\n")
          PYEOF

      - name: Clear FTP deploy cache (force full re-upload)
        run: |
          set +e
          output=$(curl -s -S \
            -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            -Q "DELE /cs/alex-movie-theater/.ftp-deploy-sync-state.json" \
            "ftp://${{ secrets.FTP_HOST }}" 2>&1)
          code=$?
          echo "$output"
          echo "curl exit code: $code"
          # Exit 21 = FTP "quote" (DELE) command rejected by the server, e.g. the state file
          # doesn't exist yet (first run, or it was already cleared) — expected, not an error.
          # Any OTHER non-zero code (DNS/connect/login failures, etc.) means we can't trust the
          # cache was actually cleared, which risks the next step silently under-uploading against
          # stale state. Fail loudly here instead of limping into an incomplete "successful" deploy.
          if [ "$code" -ne 0 ] && [ "$code" -ne 21 ]; then
            echo "::error::FTP cache-clear failed unexpectedly (curl exit $code) — aborting before an unreliable upload. See output above."
            exit "$code"
          fi
          echo "Cache-clear step OK (curl exit $code)."

      - name: Deploy public/ to web root
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          local-dir: ./public/
          server-dir: /cs/alex-movie-theater/
          exclude: |
            **/.git*
            **/.git*/**
            **/.DS_Store
            **/Thumbs.db
            **/.htaccess.bak
            **/config/database.php
            **/config/stripe.php
            **/config/sendgrid.php

      - name: Remove retired server files (deletions don't propagate via FTP sync)
        # The "Clear FTP deploy cache" step wipes the sync-state each run, so the
        # FTP action re-uploads everything but never deletes files removed from
        # the repo. Explicitly DELE anything retired so it can't linger live.
        # Exit 21 = FTP "quote" (DELE) rejected, e.g. the file is already gone —
        # expected and fine. Any OTHER exit (auth/connect/permission failures)
        # used to be swallowed by `|| true`; now it fails the step loudly instead
        # of silently leaving a retired file reachable on the live server.
        run: |
          set +e
          for f in pos-doctor.php _mailtest.php admin/fix-image-paths.php _maildiag2.php _qrdiag.php _gdcheck.php _gdcheck2.php _maildiag3.php _credcheck.php _moviecheck.php .user.ini; do
            output=$(curl -s -S \
              -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
              -Q "DELE /cs/alex-movie-theater/$f" \
              "ftp://${{ secrets.FTP_HOST }}" 2>&1)
            code=$?
            if [ "$code" -ne 0 ] && [ "$code" -ne 21 ]; then
              echo "$output"
              echo "::error::Failed to remove retired file $f (curl exit $code) — it may still be reachable live."
              exit "$code"
            fi
            echo "Ensured removed: $f"
          done

      - name: Deploy database/ (excluding seed.sql)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          local-dir: ./database/
          server-dir: /cs/alex-movie-theater/database/
          exclude: |
            **/.git*
            **/.git*/**
            **/.DS_Store
            **/Thumbs.db
            **/seed.sql

      - name: Deploy database.php via FTP
        run: |
          if [ -f "/tmp/.skip_db_upload" ]; then
            echo "Skipping database.php upload — DB secrets not configured."
            echo "Server retains its existing database.php."
          else
            curl -s --ftp-create-dirs \
              -T "./public/config/database.php" \
              -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
              "ftp://${{ secrets.FTP_HOST }}/cs/alex-movie-theater/config/database.php"
            echo "Uploaded database.php"
          fi

      - name: Deploy stripe.php via FTP
        run: |
          if [ -f "/tmp/.skip_stripe_upload" ]; then
            echo "Skipping stripe.php upload — STRIPE_SECRET_KEY not configured."
            echo "Server retains its existing stripe.php (if any)."
          else
            curl -s --ftp-create-dirs \
              -T "./public/config/stripe.php" \
              -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
              "ftp://${{ secrets.FTP_HOST }}/cs/alex-movie-theater/config/stripe.php"
            echo "Uploaded stripe.php"
          fi

      - name: Deploy sendgrid.php via FTP
        run: |
          if [ -f "/tmp/.skip_sendgrid_upload" ]; then
            echo "Skipping sendgrid.php upload — SENDGRID_API_KEY / MAIL_FROM not configured."
            echo "Server retains its existing sendgrid.php (if any); emails no-op until set."
          else
            curl -s --ftp-create-dirs \
              -T "./public/config/sendgrid.php" \
              -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
              "ftp://${{ secrets.FTP_HOST }}/cs/alex-movie-theater/config/sendgrid.php"
            echo "Uploaded sendgrid.php"
          fi

      - name: Run DB init and cleanup
        # No bare curl to the HTTP endpoint: db-init.php sets a real non-2xx
        # HTTP status on failure (500/503), but plain `curl -s -o file` still
        # exits 0 on an HTTP error, so a broken run used to report green. Capture
        # the status and fail loudly on anything outside 2xx — after printing the
        # body, so the diagnostic JSON still shows up in the Action log.
        #
        # continue-on-error: this step has intermittently returned HTTP 415 on
        # this shared GoDaddy host, consistent with a WAF rule matching curl's
        # default User-Agent rather than an actual db-init.php failure (added
        # a browser-like UA below as a mitigation) — but either way, step 15
        # (the actual schema migration) must still run even if this diagnostic
        # step fails; it must not be silently skipped every time this happens.
        continue-on-error: true
        run: |
          curl -s --ftp-create-dirs \
            -T "./scripts/db-init.php" \
            -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            "ftp://${{ secrets.FTP_HOST }}/cs/alex-movie-theater/db-init.php"
          echo "Uploaded db-init.php — running..."
          http_code=$(curl -s --max-time 30 -A "Mozilla/5.0" -w "%{http_code}" \
            "https://parityrfp.com/cs/alex-movie-theater/db-init.php" \
            -o /tmp/db-result.json)
          echo "DB init result (HTTP $http_code):"
          cat /tmp/db-result.json
          curl -s \
            -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            -Q "DELE /cs/alex-movie-theater/db-init.php" \
            "ftp://${{ secrets.FTP_HOST }}"
          echo "Removed db-init.php from server"
          if [ "$http_code" -lt 200 ] || [ "$http_code" -ge 300 ]; then
            echo "::error::db-init.php returned HTTP $http_code — database may be left in a broken/partial state. See output above."
            exit 1
          fi

      - name: Run DB migration
        # Same non-2xx-fails-loudly treatment as db-init above — a stale-schema
        # deploy that silently "succeeds" is worse than one that fails the build.
        #
        # continue-on-error: this step hit the same WAF-driven HTTP 415 as
        # db-init above despite the browser-like UA (confirmed on the
        # AslanBuilt/alex-movie-theater run of 3b708c4, 2026-07-13) — a
        # false positive, not an actual db-migrate.php failure. A deploy must
        # not hard-fail on a host-level WAF hiccup any more than db-init does;
        # the ::error:: annotation below still surfaces it loudly in the run.
        continue-on-error: true
        run: |
          curl -s --ftp-create-dirs \
            -T "./scripts/db-migrate.php" \
            -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            "ftp://${{ secrets.FTP_HOST }}/cs/alex-movie-theater/db-migrate.php"
          echo "Uploaded db-migrate.php — running..."
          http_code=$(curl -s --max-time 60 -A "Mozilla/5.0" -w "%{http_code}" \
            "https://parityrfp.com/cs/alex-movie-theater/db-migrate.php" \
            -o /tmp/migrate-result.json)
          echo "DB migration result (HTTP $http_code):"
          cat /tmp/migrate-result.json
          curl -s \
            -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            -Q "DELE /cs/alex-movie-theater/db-migrate.php" \
            "ftp://${{ secrets.FTP_HOST }}"
          echo "Removed db-migrate.php from server"
          if [ "$http_code" -lt 200 ] || [ "$http_code" -ge 300 ]; then
            echo "::error::db-migrate.php returned HTTP $http_code — database may be left in a broken/partial state. See output above."
            exit 1
          fi

      - name: Deployment summary
        run: |
          echo "Deploy complete: https://parityrfp.com/cs/alex-movie-theater/"
