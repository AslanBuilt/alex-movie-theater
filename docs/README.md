# alex-movie-theater — docs map

- `requirements/` — personas, EARS requirements (REQ-*), flow registry + RTM (the WHAT, traces to SOW)
- `decisions/` — ADRs (immutable, numbered; engineering/industry/preference calls — see `decision-records`)
- `specs/` — design specs + mockups (the HOW)
- `plans/` — implementation plans (tasks cite REQ + SCREEN)
- `context/` — client decisions log, open questions, changelog, meeting notes

Traceability model + tiering matrix: the `requirements-traceability` skill.
Audit (report): `node <aslan-skills>/scripts/traceability-audit.mjs docs`
